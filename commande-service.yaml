server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tiaktiak_commande
    username: root
    password: mary2003
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

tiaktiak:
  commande:
    concepteur: XOF
    montant-minimum: 1000
